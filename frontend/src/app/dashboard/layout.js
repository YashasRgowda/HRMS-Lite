export const metadata = {
  title: "Dashboard | HRMS Lite",
  description: "Live attendance summary and HR statistics",
};

export default function DashboardLayout({ children }) {
  return children;
}